
const ChatMessage = () => {
    const currentsendermsg = false
    const currentsendermsg1 = true
    return (
        <div>
            <div className={`flex ${currentsendermsg ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg ? 'bg-yellow-200 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message received by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg1 ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg1 ? 'bg-yellow-100 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message send by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg ? 'bg-yellow-200 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message received by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg1 ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg1 ? 'bg-yellow-100 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message send by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg ? 'bg-yellow-200 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message received by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg1 ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg1 ? 'bg-yellow-100 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message send by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg ? 'bg-yellow-200 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message received by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg1 ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg1 ? 'bg-yellow-100 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message send by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg ? 'bg-yellow-200 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message received by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg1 ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg1 ? 'bg-yellow-100 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message send by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg ? 'bg-yellow-200 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message received by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg1 ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg1 ? 'bg-yellow-100 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message send by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg ? 'bg-yellow-200 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message received by the current user</p>
                </div>
            </div>
            <div className={`flex ${currentsendermsg1 ? 'justify-end' : 'justify-start'} mb-1.5`}>
                <div className={`p-2 ${currentsendermsg1 ? 'bg-yellow-100 border-2 border-amber-300 rounded-sm' : 'bg-green-200 border-2 border-green-800 rounded-sm'}`}>
                    <p>This is a message send by the current user</p>
                </div>
            </div>
        </div>
    )
}

export default ChatMessage